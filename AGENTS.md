# Agent Instructions for wife_treament_process

This is a minimal static workspace containing a single HTML page: `index.html`.

- The project is a static frontend only.
- There is no package manager, build system, backend, or tests present.
- Edit `index.html` directly for UI, structure, markup, and inline styles/scripts.
- Do not assume React, Vue, Angular, or any JS framework.
- Do not add new tooling unless explicitly requested by the user.
- Keep changes small and focused when modifying the single-file project.

If the user asks for new frontend behavior, implement it directly in `index.html` and explain that this workspace currently has no separate CSS/JS files or build setup.